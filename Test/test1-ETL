import pytest
from etl import extract-data, transform-data, load-data

df = read_csv("data/input_data.csv")

def extract-data(data/input_data.csv):
    assert not df.empty
    assert list(df.columns) == ["id","name","age","city","salary"]
    assert df.shape == (5, 7)
    

# def transform-data():
#     assert subtract(5, 3) == 2
#     assert subtract(2, 5) == -3

# def load-data():
#     assert multiply(2, 3) == 6
#     assert multiply(-1, 3) == -3

